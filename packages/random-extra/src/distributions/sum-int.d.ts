import { IRNGLike } from '@lazy-random/rng-abstract';
declare const _default: (random: IRNGLike, size: number, sum?: number, min?: number, max?: number, limit?: number) => () => number[];
export default _default;
