import { fixZero } from 'num-is-zero';
export { fixZero };
export declare function get_range_by_size_sum(size: number, sum?: number): {
    min: number;
    max: number;
    sum: number;
    resultArray: number[];
};
