
const randomSeedStrData = Object.freeze({
	"name": "random-extra",
	"version": "3.6.15"
});

export { randomSeedStrData }

export const name = randomSeedStrData.name;
export const version = randomSeedStrData.version;

export default randomSeedStrData
