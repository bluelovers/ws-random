export declare function toFixedStringNumber(n: number, fractionDigits: number): string;
/**
 * a number using fixed-point notation
 */
export declare function toFixedNumber(n: number, fractionDigits: number): number;

export {
	toFixedNumber as default,
};

export {};
